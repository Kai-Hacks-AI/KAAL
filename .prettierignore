# Nodes are immutable: never reformat BRAIN.
brain/
# Test data must stay byte-exact (CRLF, missing final newline, whitespace names).
skills/using-brain/test-data/
# Seal fixtures hash exact bytes: reformatting would break them.
skills/using-seals/test-data/
# Guidance fixtures keep exact bytes (CRLF, blank guidance).
skills/using-agents/test-data/
# Example skills keep exact bytes: using-skills compares SKILL.md to what init generates.
skills/using-skills/test-data/
# KAAL's seal fixtures: same reason.
test-data/
package-lock.json
# Generated by each skill's init: init is the source, never reformat its output.
skills/*/SKILL.md
# Created by using-agents from a using system's guidance: never reformat it.
AGENTS.md
