import fs from "node:fs";import path from "node:path";import YAML from "yaml";import { pathToFileURL } from "node:url";import type { Edge } from "./brain.js";
export type CreateNode={root?:string;lineage:string;learning:string;slug:string;name:string;meaning:string;edges?:Edge[]};
function component(v:string,label:string){if(!/^[A-Za-z0-9][A-Za-z0-9._-]*$/.test(v)||v==="."||v==="..")throw new Error(`${label} must be one path component`);}
export function createNode(input:CreateNode):string{const root=input.root??"brain/learning";component(input.lineage,"lineage");component(input.slug,"slug");if(!/^\d{2}\/\d{2}\/\d{2}\/\d{2}$/.test(input.learning))throw new Error("learning must be YY/MM/DD/CC");const dir=path.join(root,input.lineage,input.learning,"nodes"),file=path.join(dir,`${input.slug}.md`);if(fs.existsSync(file))throw new Error(`${file}: already exists`);fs.mkdirSync(dir,{recursive:true});const fm=input.edges?.length?{name:input.name,edges:input.edges}:{name:input.name};fs.writeFileSync(file,`---\n${YAML.stringify(fm).trimEnd()}\n---\n\n${input.meaning.trim()}\n`,{flag:"wx"});return file;}
if(process.argv[1]&&import.meta.url===pathToFileURL(process.argv[1]).href){const [lineage,learning,slug,name,meaning]=process.argv.slice(2);if(!lineage||!learning||!slug||!name||!meaning){console.error("usage: create-node.ts <lineage> <YY/MM/DD/CC> <slug> <name> <meaning>");process.exitCode=2}else createNode({lineage,learning,slug,name,meaning});}
